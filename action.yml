name: 'Generate Issue'
description: 'Parse a Pull Request and generate a new GitHub issue if needed'
branding:
  icon: 'bar-chart'  
  color: 'blue'
inputs:
  myToken:  # GITHUB_TOKEN
    description: 'The GitHub auth token'
    required: true
    default: 'Test'
runs:
  using: 'node12'
  main: 'dist/index.js'
